package test.main;

public class MainClass03 {
	public static void main(String[] args) {
		/*
		 * new 배열안에 저장할 데이터 type [방의 갯수]
		 * 와 같은 형식으로 배열객체를 생성할 수 있다.
		 */
		int[] nums = new int[5];
		// 방 5개짜리 배열 객체가 생성된걸 알 수 가있다.
		int length = nums.length;
		
		//String type 을 담을 수 있는 방 10개짜리 배열객체를 생성해서 그 참조값을
		//name이라는 지역변수에 담아보세요.
		String[] names = new String[10];
	}
}
